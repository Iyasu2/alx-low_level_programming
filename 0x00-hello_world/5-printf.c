#include <stdio.h>
/**
 * main - Entry point
 * Description: 'printing using printf'
 * Parameters: no input returns an int
 * Return: Always 0 (Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");

	return (0);
}
